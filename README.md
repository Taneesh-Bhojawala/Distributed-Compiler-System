# Distributed-Compiler-System
A central system that accepts codes in different languages (C, C++,Java) and distributes the compilation work load across different systems on the network to facilitate faster compilation.
